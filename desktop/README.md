# Desktop frontend (Flutter — admin)

Flutter **Windows** aplikacija za administratore. Paket u ovom folderu — nakon instalacije Flutter SDK-a:

```powershell
flutter create . --platforms=windows
flutter run -d windows --dart-define=API_BASE_URL=http://localhost:5000
```

Mobilni klijent je u `../mobile/`.
